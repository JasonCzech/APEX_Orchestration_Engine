apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "apex.fullname" . }}-settings
  labels:
    {{- include "apex.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.apexSettings }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
